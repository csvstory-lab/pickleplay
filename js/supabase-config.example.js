/**
 * Supabase 연결 정보 (예시 파일)
 *
 * 사용법:
 * 1. 이 파일을 복사해서 같은 폴더에 supabase-config.js 로 저장
 * 2. url, anonKey 를 Supabase 대시보드(Settings → API) 값으로 바꿈
 *
 * ⚠️ supabase-config.js 는 Git에 올리지 마세요 (.gitignore 처리됨)
 */
window.PICKLE_SUPABASE_CONFIG = {
  url: 'https://YOUR_PROJECT_REF.supabase.co',
  anonKey: 'YOUR_PUBLISHABLE_OR_ANON_KEY',
};
