-- =============================================================================
-- P!CKLE 데모 데이터 — 이 파일은 더 이상 한 번에 실행하지 마세요.
--
-- Supabase SQL Editor 복사 잘림 방지를 위해 2개로 나눴습니다:
--
--   1) 04a_demo_cleanup.sql  ← 먼저 Run (청소)
--   2) 04b_demo_seed.sql     ← 그다음 Run (설치)
--
-- 각 파일을 메모장으로 열어 Ctrl+A → Ctrl+C → SQL Editor → Run
-- =============================================================================
